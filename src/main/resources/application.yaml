server:
  port: 8081

spring:
  application:
    name: ekklesia-admin-api

  profiles:
    active: dev   # padrão

jwt:
  secret: your-super-secret-key-change-this
  expiration: 86400000

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger
